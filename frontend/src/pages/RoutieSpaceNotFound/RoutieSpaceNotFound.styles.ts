import { css } from '@emotion/react';

import theme from '@/styles/theme';

const ErrorPageContainerStyle = css`
  min-height: 100dvh;
  padding: 4rem 2rem;
  text-align: center;
  background-color: ${theme.colors.white};
`;

export { ErrorPageContainerStyle };
