spring:
  config:
    activate:
      on-profile: test

  profiles:
    active: test
  
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MYSQL
    username: test
    password: test

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.H2Dialect
    show-sql: true

  flyway:
    enabled: true

kakao:
  api:
    key: test-api-key

google:
  api:
    key: test-api-key

v-world:
  api:
    key: test-api-key

authentication:
  jwt:
    secret: 02ff097348d559b2d5df6cdb91e7fd05f19b1ded7dbefeed1e6209db9a63987a
    expiration: 604800000 # 7 days
  external:
    kakao:
      client-id: client-id
      redirect-uri: redirect-uri
