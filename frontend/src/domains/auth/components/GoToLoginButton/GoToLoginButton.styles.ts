import { css } from '@emotion/react';

import theme from '@/styles/theme';

const GoToLoginButtonStyle = css`
  width: 50rem;
  background-color: ${theme.colors.white};
  box-shadow: 0 0.8rem 1.6rem rgb(0 0 0 / 26%);
`;
export { GoToLoginButtonStyle };
