spring:
  config:
    activate:
      on-profile: test

  profiles:
    active: test
  
  datasource:
    url: jdbc:h2:mem:testdb
    username: test
    password: test

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.H2Dialect
    show-sql: true

kakao:
  api:
    key: test-api-key

v-world:
  api:
    key: test-api-key
