import { css } from '@emotion/react';

import theme from '@/styles/theme';

const BannerContainerStyle = css`
  width: 100%;
  height: 20rem;
  background-color: ${theme.colors.blue[250]};
`;

export { BannerContainerStyle };
