import { css } from '@emotion/react';

import theme from '@/styles/theme';

const SearchNoticeStyle = css`
  position: relative;
  height: 100%;
  color: ${theme.colors.black};
`;

export { SearchNoticeStyle };
