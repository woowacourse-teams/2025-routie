spring:
  config:
    activate:
      on-profile: test

  profiles:
    active: test
  
  datasource:
    url: jdbc:mysql://test-db.routie.me:443/routie?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: routie-test
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        jdbc:
          batch_size: 1000
          order_inserts: true
          order_updates: true
        format_sql: true
    #        dialect: org.hibernate.dialect.H2Dialect
  #    show-sql: true
decorator:
  datasource:
    p6spy:
      enable-logging: false

  flyway:
    enabled: true
    locations: classpath:db/migration

kakao:
  api:
    key: test-api-key

google:
  api:
    key: test-api-key

v-world:
  api:
    key: test-api-key
