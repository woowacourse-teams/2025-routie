import { css } from '@emotion/react';

import theme from '@/styles/theme';

const ProfileButtonStyle = css`
  cursor: pointer;
  width: fit-content;
  background-color: ${theme.colors.white};
`;

export { ProfileButtonStyle };
